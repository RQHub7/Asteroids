[project]
name = "Asteroids"
version = "0.1.0"
description = "Basic version of the classic - might spinoff and modify later on..."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
